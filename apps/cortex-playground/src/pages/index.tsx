import { Root } from "@/components/Root";
import { CreateAIForm } from "@instill-ai/toolkit";

const IndexPage = () => {
  return (
    <Root>
      <CreateAIForm />
    </Root>
  );
};

export default IndexPage;
